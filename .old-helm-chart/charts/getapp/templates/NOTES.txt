1. Access the getaa application using the following URLs:

API URL:
--------
https://api-{{ .Values.routePrefix }}.{{ .Values.routeMainlUrl }}

Swagger UI URL:
--------------
https://api-{{ .Values.routePrefix }}.{{ .Values.routeMainlUrl }}/docs/

Dashboard URL:
-------------
https://dashboard-{{ .Values.routePrefix }}.{{ .Values.routeMainlUrl }}

Note: These URLs are configured through OpenShift routes and use TLS edge termination.
